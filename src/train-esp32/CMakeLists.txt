# Top-level CMakeLists.txt
cmake_minimum_required(VERSION 3.5)

# Include the ESP-IDF cmake file that initializes the build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Project name
project(train_control_system)
